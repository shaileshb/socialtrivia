
Run the following command to configure the local android build environment.

$ ../tools/android-sdk/tools/android update project --name SocialTrivia --target 1 --path .

Then compile the project locally using ant to generate the resource files.

$ ant debug

